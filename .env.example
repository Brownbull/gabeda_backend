# Django Settings
DJANGO_SECRET_KEY=change-me-in-production-use-long-random-string
DJANGO_DEBUG=True
DJANGO_SETTINGS_MODULE=config.settings.local

# Database (SQLite for local, PostgreSQL for production)
DATABASE_URL=sqlite:///db.sqlite3

# Redis (Celery background tasks)
REDIS_URL=redis://localhost:6379/0

# GabeDA Analytics Engine Path
GABEDA_SRC_PATH=C:/Projects/play/khujta_ai_business

# Allowed Hosts & CORS
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# JWT Settings (optional overrides)
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=1440  # 24 hours
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Email (for password reset, optional for MVP)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-app-password

# Logging
LOG_LEVEL=INFO
